name: Reset sandbox to main
on:
  schedule:
  - cron: "50 23 * * 0"
  workflow_dispatch:
jobs:
  reset-sandbox:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          ref: 'main'
      - run: git push -f origin main:sandbox
